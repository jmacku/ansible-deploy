---
certbot_email: "devops+certbot@kardi-ai.com"
certbot_staging: false
aws_region: "eu-central-1"

# Base domain — each host gets at minimum its own hostname
# Override per host_vars to add extra domains (e.g. de.mon has status.kardi-ai.com)
certbot_domains:
  - "{{ inventory_hostname }}"
