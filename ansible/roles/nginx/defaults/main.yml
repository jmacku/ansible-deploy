# yaml
nginx_repo_url: "https://nginx.org/packages/mainline/ubuntu"
nginx_repo_key_url: "https://nginx.org/keys/nginx_signing.key"
nginx_version: "stable"  # options: stable, mainline

nginx_package_name: nginx

# nginx.org packages use nginx:nginx
nginx_user: nginx
nginx_group: nginx
